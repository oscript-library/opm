// Заглушка ответа хаба: код, тело и заголовки задаёт тест — сеть в тестах не нужна.

Перем КодСостояния Экспорт;
Перем Заголовки Экспорт;
Перем Тело;

// Задаёт ответ, который увидит менеджер получения пакетов.
// Заголовки при этом обнуляются: заглушку переиспользуют в одном тесте, и оставшаяся
// от прошлого ответа контрольная сумма прикрыла бы дефект в её проверке.
//
// Параметры:
//  Код - Число - код состояния HTTP.
//  ТелоОтвета - Строка - содержимое файла пакета.
//
Процедура Отвечать(Знач Код, Знач ТелоОтвета = "") Экспорт

	КодСостояния = Код;
	Тело = ТелоОтвета;
	Заголовки = Новый Соответствие();

КонецПроцедуры

// Объявляет контрольную сумму так, как это делает хаб.
//
// Параметры:
//  Сумма - Строка - значение заголовка X-Checksum-Sha256.
//
Процедура ОбъявитьКонтрольнуюСумму(Знач Сумма) Экспорт

	Заголовки.Вставить("X-Checksum-Sha256", Сумма);

КонецПроцедуры

Функция ПолучитьТелоКакДвоичныеДанные() Экспорт

	Возврат ПолучитьДвоичныеДанныеИзСтроки(Тело);

КонецФункции

Процедура Закрыть() Экспорт
	// для совместимости контракта
КонецПроцедуры

КодСостояния = 200;
Заголовки = Новый Соответствие();
Тело = "";
