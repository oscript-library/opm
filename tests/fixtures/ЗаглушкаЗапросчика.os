// Заглушка ЗапросчикHTTP: заранее заданный ответ и журнал запросов — сеть в тестах не нужна.

Перем Ответ;
Перем Запросы Экспорт;

// Задаёт ответ, который вернёт следующий запрос.
//
// Параметры:
//  Код - Число - код состояния; 0 означает «до сервера не дозвонились».
//  Тело - Строка - тело ответа.
//
Процедура Отвечать(Знач Код, Знач Тело) Экспорт

	Ответ = Новый Структура("Код, Тело", Код, Тело);

КонецПроцедуры

Функция Получить(Знач Адрес, Знач Заголовки) Экспорт

	Запросы.Добавить(Новый Структура("Адрес, Заголовки", Адрес, Заголовки));

	Возврат Ответ;

КонецФункции

Ответ = Новый Структура("Код, Тело", 200, "{}");
Запросы = Новый Массив();
