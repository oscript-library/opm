// Заглушка ТокенКонвейераCI: отвечает заранее заданным вердиктом и считает обращения.

Перем Обнаружение Экспорт;
Перем Результат Экспорт;
Перем ОбращенийБыло Экспорт;
Перем ПоследняяАудитория Экспорт;
Перем ПоследнееИмяПеременной Экспорт;

// Задаёт, что заглушка «видит» вокруг себя.
//
// Параметры:
//  Найдено - Булево - виден ли конвейер с id-token.
//  Причина - Строка - объяснение находки либо её отсутствия.
//  ИмяПеременной - Строка - переменная среды, в которой лежит id-token.
//
Процедура Обнаруживать(Знач Найдено, Знач Причина = "проба", Знач ИмяПеременной = "") Экспорт

	Обнаружение = Новый Структура("Найдено, Способ, ИмяПеременной, Причина",
		Найдено, ?(ПустаяСтрока(ИмяПеременной), "сервис", "переменная"), ИмяПеременной, Причина);

КонецПроцедуры

// Задаёт вердикт, который вернёт получение токена.
//
// Параметры:
//  Успех - Булево - удалось ли получить токен.
//  Токен - Строка - значение id-token.
//  Сообщение - Строка - причина неудачи.
//
Процедура Отдавать(Знач Успех, Знач Токен, Знач Сообщение = "") Экспорт

	Результат = Новый Структура("Успех, Токен, Источник, Сообщение",
		Успех, Токен, "заглушка", Сообщение);

КонецПроцедуры

Функция Обнаружить(Знач ИмяПеременной = "") Экспорт

	Возврат Обнаружение;

КонецФункции

Функция Получить(Знач Аудитория, Знач ИмяПеременной = "") Экспорт

	ОбращенийБыло = ОбращенийБыло + 1;
	ПоследняяАудитория = Аудитория;
	ПоследнееИмяПеременной = ИмяПеременной;

	Возврат Результат;

КонецФункции

ОбращенийБыло = 0;
ПоследняяАудитория = "";
ПоследнееИмяПеременной = "";
Обнаруживать(Ложь, "заглушка ничего не видит");
Результат = Новый Структура("Успех, Токен, Источник, Сообщение", Ложь, "", "заглушка", "не задан вердикт");
