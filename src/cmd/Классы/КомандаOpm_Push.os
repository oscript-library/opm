#Использовать logos
#Использовать gitrunner

Перем ДопустимыеИменаКаналов;
Перем Лог;

///////////////////////////////////////////////////////////////////////////////////////////////////
// Прикладной интерфейс

Процедура ОписаниеКоманды(Знач КомандаПриложения) Экспорт

	КомандаПриложения.Опция("hub", "", "Адрес хаба публикации: [схема://]хост[:порт][/пул].
								|                        Не указан — берётся описанный в opm.cfg сервер,
								|                        а если и его нет — хаб по умолчанию.")
								.ВОкружении("OPM_HUB");

	КомандаПриложения.Опция("token", "", "Токен авторизации в хабе пакетов.
								|                        - Для OpenHub это персональный токен доступа (PAT), выданный opm login.
								|                        - Для hub.oscript.io — токен GitHub со страницы https://github.com/settings/tokens,
								|                        он используется только для проверки прав на репозиторий,
								|                        выдача дополнительных разрешений в ""scopes"" НЕ требуется.")
								.ВОкружении("GITHUB_OAUTH_TOKEN");
	;
	КомандаПриложения.Опция("token-stdin", Ложь, "Прочитать токен из стандартного ввода, мимо истории команд.");
	КомандаПриложения.Опция("force", Ложь, "Опубликовать поверх уже занятого номера версии.
								|                        Хаб может отклонить повторную публикацию номера;
								|                        какие номера защищены, решает он сам.");
	КомандаПриложения.Опция("f file", "", "УСТАРЕЛА. Маска или имя файла пакета — указывайте аргументом.");
	КомандаПриложения.Опция("c channel", "auto", "УСТАРЕЛА: канал версии хаб определяет тегами пакета.
							|    Принимает имя канала (stable, dev); адрес пула указывается опцией <--hub>.")
							.ВОкружении("OPM_HUB_CHANNEL");
	ОпцияЗеркала = КомандаПриложения.Опция("m mirror", "", "УСТАРЕЛА. Имя сервера для публикации из opm.cfg.
							|    Вместо неё указывайте адрес пула опцией <--hub>: [схема://]хост[:порт]/пул.")
							.ВОкружении("OPM_HUB_MIRROR")
							.ТПеречисление();

	МенеджерПолучения = Новый МенеджерПолученияПакетов();
	Для Каждого ДоступноеЗеркало Из МенеджерПолучения.ИменаДоступныхСерверов() Цикл
		ОпцияЗеркала.Перечисление(ДоступноеЗеркало, ДоступноеЗеркало, "Сервер '" + ДоступноеЗеркало + "'");
	КонецЦикла;

	ОписатьОпцииДоверия(КомандаПриложения);

	КомандаПриложения.Аргумент("FILE", "", "Файл пакета либо маска файлов.
							|    По умолчанию — единственный *.ospx в текущем каталоге.")
							.Обязательный(Ложь);

КонецПроцедуры

Процедура ВыполнитьКоманду(Знач КомандаПриложения) Экспорт

	МаскаФайлаПакета = МаскаФайлаПакетаИзКоманды(КомандаПриложения);
	ИмяСервераПакетов = КомандаПриложения.ЗначениеОпции("mirror");
	ИмяКанала = ИмяКаналаИзКоманды(КомандаПриложения);
	АдресХаба = СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("hub")));

	ПараметрыПриложенияOpm.ПредупредитьОбУстаревшемЗеркале(ИмяСервераПакетов);

	ФайлПакета = ПолучитьФайлПакета(МаскаФайлаПакета);
	Цель = ЦельПубликации(АдресХаба, ИмяСервераПакетов);

	Лог.Отладка("Публикация в %1, ресурс %2 (%3)", Цель.Сервер, Цель.Ресурс, Цель.Источник);

	Канал = ПолучитьИмяКаналаПубликации(ИмяКанала, Цель.Легаси);
	АвторизацияПубликации = ВыбратьАвторизацию(КомандаПриложения, Цель);

	Опубликовать(Цель, АвторизацияПубликации, ФайлПакета, Канал,
		ЭтоПринудительнаяПубликация(КомандаПриложения));

КонецПроцедуры

// Куда публикуем: адрес из команды, описанный в opm.cfg сервер либо хаб по умолчанию.
//
// Параметры:
//  АдресХаба - Строка - значение опции <--hub>; пустое отправляет за целью в настройки.
//  ИмяСервераПакетов - Строка - имя сервера из opm.cfg (устаревшая опция <-m|--mirror>).
//
// Возвращаемое значение:
//  Структура - Имя, Сервер, Порт, Ресурс, Пул, БазовыйАдрес, Таймаут, Авторизация,
//              Заголовки, Источник, Легаси.
//
Функция ЦельПубликации(Знач АдресХаба, Знач ИмяСервераПакетов = "") Экспорт

	Если ЗначениеЗаполнено(АдресХаба) Тогда
		Возврат ЦельИзАдреса(АдресХаба);
	КонецЕсли;

	Если ЗначениеЗаполнено(ИмяСервераПакетов) Тогда
		Настройка = НайтиНастройкуСервера(ИмяСервераПакетов);
		Если Настройка <> Неопределено Тогда
			ПроверитьПригодностьКПубликации(Настройка, ИмяСервераПакетов);
			Возврат ЦельИзНастройки(Настройка, СтрШаблон("сервер <%1> из opm.cfg", ИмяСервераПакетов));
		КонецЕсли;
	КонецЕсли;

	Настройка = ПерваяНастройкаДляПубликации();

	Если Настройка <> Неопределено Тогда
		Цель = ЦельИзНастройки(Настройка, СтрШаблон("сервер <%1> из настроек", Настройка.Имя));
		Лог.Информация("Адрес хаба не указан: публикую в %1 (%2)", Цель.Сервер, Цель.Источник);
		Возврат Цель;
	КонецЕсли;

	Лог.Информация("Адрес хаба не указан и в настройках его нет: публикую в %1",
		КонстантыOpm.СерверУдаленногоХранилища);

	Возврат НоваяЦель("хаб по умолчанию", Истина);

КонецФункции

// Чем авторизуемся в хабе. Доверенная публикация решается набором переменных среды
// и идёт первой: раз конвейер выдал id-token, публикуют именно им, а токен нужен там,
// где доверия вокруг не видно.
//
// Параметры:
//  КомандаПриложения - КомандаПриложения - выполняемая команда.
//  Цель - Структура - см. ЦельПубликации.
//  Конвейер - ТокенКонвейераCI - источник id-token; Неопределено — окружение процесса.
//
// Возвращаемое значение:
//  Структура - Заголовок (Строка), Значение (Строка), Доверенная (Булево).
//
Функция ВыбратьАвторизацию(Знач КомандаПриложения, Знач Цель, Знач Конвейер = Неопределено) Экспорт

	Доверие = ПараметрыДоверия(КомандаПриложения);

	ТокенКонвейера = ТокенДоверенногоКонвейера(Доверие.ИмяПеременной, Доверие.Аудитория,
		Цель.БазовыйАдрес, Конвейер);

	Если НЕ ПустаяСтрока(ТокенКонвейера) Тогда
		Возврат НоваяАвторизация("Authorization", "Bearer " + ТокенКонвейера, Истина);
	КонецЕсли;

	ТокенВКоманде = ТокенИзКоманды(КомандаПриложения);
	Если НЕ ПустаяСтрока(ТокенВКоманде) Тогда
		Возврат АвторизацияТокеном(ТокенВКоманде, Цель);
	КонецЕсли;

	Если ЗначениеЗаполнено(Цель.Авторизация) Тогда
		Лог.Отладка("Авторизация взята из описания сервера (%1)", Цель.Источник);
		Возврат НоваяАвторизация("Authorization",
			ОбщегоНазначенияOpm.ПрименитьПеременныеСреды(Цель.Авторизация), Ложь);
	КонецЕсли;

	ТокенИзНастроек = ТокенПубликацииИзНастроек();
	Если НЕ ПустаяСтрока(ТокенИзНастроек) Тогда
		Лог.Отладка("Токен публикации взят из файла настроек");
		Возврат АвторизацияТокеном(ТокенИзНастроек, Цель);
	КонецЕсли;

	ВызватьИсключение СтрШаблон("Для публикации в %1 нужен токен: %2."
		+ " Доверенная публикация не применяется: %3",
		Цель.Имя, ГдеВзятьТокен(Цель), ПричинаОтсутствияДоверия(Доверие.ИмяПеременной, Конвейер));

КонецФункции

// Токен доверенного конвейера либо пустая строка, если доверенной публикации вокруг нет.
// Названная опцией переменная — заявленное намерение: её отсутствие останавливает команду.
//
// Параметры:
//  ИмяПеременной - Строка - имя переменной среды с id-token (опция <--oidc-token-var>).
//  Аудитория - Строка - клейм aud; пустая берётся равной адресу хаба публикации.
//  БазовыйАдресХаба - Строка - адрес хаба публикации.
//  Конвейер - ТокенКонвейераCI - источник id-token; Неопределено — окружение процесса.
//
// Возвращаемое значение:
//  Строка - значение id-token либо пустая строка.
//
Функция ТокенДоверенногоКонвейера(Знач ИмяПеременной, Знач Аудитория, Знач БазовыйАдресХаба,
		Знач Конвейер = Неопределено) Экспорт

	Источник = ?(Конвейер = Неопределено, Новый ТокенКонвейераCI(), Конвейер);
	Обнаруженное = Источник.Обнаружить(ИмяПеременной);

	Если НЕ Обнаруженное.Найдено Тогда
		Если НЕ ПустаяСтрока(СокрЛП(Строка(ИмяПеременной))) Тогда
			ВызватьИсключение СтрШаблон("Доверенная публикация не состоялась: %1", Обнаруженное.Причина);
		КонецЕсли;
		Лог.Отладка("Доверенная публикация не применяется: %1", Обнаруженное.Причина);
		Возврат "";
	КонецЕсли;

	ЗаказаннаяАудитория = ?(ПустаяСтрока(Аудитория), БазовыйАдресХаба, Аудитория);

	Лог.Информация("Публикую доверенным конвейером: %1. Аудитория id-token — %2",
		Обнаруженное.Причина, ЗаказаннаяАудитория);

	Предупреждение = ПредупреждениеОбАудитории(ЗаказаннаяАудитория, БазовыйАдресХаба);
	Если НЕ ПустаяСтрока(Предупреждение) Тогда
		Лог.Предупреждение(Предупреждение);
	КонецЕсли;

	Результат = Источник.Получить(ЗаказаннаяАудитория, Обнаруженное.ИмяПеременной);

	Если НЕ Результат.Успех Тогда
		ВызватьИсключение СтрШаблон("Доверенная публикация не состоялась: %1", Результат.Сообщение);
	КонецЕсли;

	Лог.Отладка("Доверенная публикация: id-token получен (%1)", Результат.Источник);

	Возврат Результат.Токен;

КонецФункции

// Требует ли команда публикации поверх уже занятого номера версии: опция <--force>.
//
// Параметры:
//  КомандаПриложения - КомандаПриложения - выполняемая команда.
//
// Возвращаемое значение:
//  Булево.
//
Функция ЭтоПринудительнаяПубликация(Знач КомандаПриложения) Экспорт

	Возврат ФлагПоднят(КомандаПриложения.ЗначениеОпции("force"), "force");

КонецФункции

// Отправляет файл пакета в хаб и объясняет отказ.
//
// Параметры:
//  Цель - Структура - см. ЦельПубликации.
//  ЗначениеАвторизации - Структура - см. ВыбратьАвторизацию.
//  ФайлПакета - Файл - файл .ospx, который публикуем.
//  Канал - Строка - значение заголовка CHANNEL.
//  Принудительно - Булево - публиковать поверх уже занятого номера версии.
//
Процедура Опубликовать(Знач Цель, Знач ЗначениеАвторизации, Знач ФайлПакета, Знач Канал,
		Знач Принудительно = Ложь) Экспорт

	Заголовки = Новый Соответствие();
	Для Каждого Заголовок Из Цель.Заголовки Цикл
		Заголовки.Вставить(Заголовок.Ключ, Заголовок.Значение);
	КонецЦикла;
	Заголовки.Вставить(ЗначениеАвторизации.Заголовок, ЗначениеАвторизации.Значение);

	Отправитель = Новый ОтправительПакета(Цель.Сервер, Цель.Порт, Заголовки, Цель.Таймаут);

	Результат = Отправитель.Опубликовать(Цель.Ресурс, ФайлПакета.ПолноеИмя, Канал, Принудительно);

	Если НЕ Результат.Успех Тогда
		ВызватьИсключение СообщениеОбОтказеПубликации(Результат, ЗначениеАвторизации.Доверенная, Цель.Имя);
	КонецЕсли;

	Лог.Информация(Результат.Сообщение);

КонецПроцедуры

// Текст отказа хаба. Про доверенную публикацию говорится отдельно: поддержку доверия
// и запись доверия ведёт хаб, поэтому клиенту видно только код ответа, а человеку нужно
// знать, что не удалась именно она.
//
// Параметры:
//  Результат - Структура - ответ ОтправительПакета: Код, Сообщение, Успех.
//  Доверенная - Булево - публиковали ли id-token'ом.
//  ИмяХаба - Строка - хаб, в который публиковали.
//
// Возвращаемое значение:
//  Строка - текст исключения.
//
Функция СообщениеОбОтказеПубликации(Знач Результат, Знач Доверенная, Знач ИмяХаба) Экспорт

	Если НЕ Доверенная Тогда
		Возврат Результат.Сообщение;
	КонецЕсли;

	Возврат СтрШаблон("Доверенная публикация в %1 не удалась, хаб ответил кодом %2: %3."
		+ " Хаб может не поддерживать доверенную публикацию либо не иметь записи доверия"
		+ " для этого пакета — тогда публикуйте токеном",
		ИмяХаба, Формат(Результат.Код, "ЧГ=0; ЧН=0"), Результат.Сообщение);

КонецФункции

// Текст предупреждения о чужой аудитории. Аудитория задаётся и переменной среды, которая
// переживает смену хаба, а несовпадение хаб покажет только отказом на публикации.
//
// Параметры:
//  Аудитория - Строка - аудитория, с которой заказан id-token.
//  БазовыйАдресХаба - Строка - адрес хаба публикации.
//
// Возвращаемое значение:
//  Строка - текст предупреждения; пустая строка, если предупреждать не о чем.
//
Функция ПредупреждениеОбАудитории(Знач Аудитория, Знач БазовыйАдресХаба) Экспорт

	Если НРег(СокрЛП(Аудитория)) = НРег(СокрЛП(БазовыйАдресХаба)) Тогда
		Возврат "";
	КонецЕсли;

	Возврат СтрШаблон("Аудитория id-token <%1> не совпадает с адресом хаба публикации <%2>:"
		+ " хаб сверяет клейм aud со своим адресом и такую публикацию отклонит",
		Аудитория, БазовыйАдресХаба);

КонецФункции

///////////////////////////////////////////////////////////////////////////////////////////////////

// Значение опции-флага в булево: cli отдаёт поднятый флаг строкой "True", неподнятый —
// булевой Ложью, а <--force=значение> — как написано. Непонятое значение — внятный отказ.
Функция ФлагПоднят(Знач Значение, Знач ИмяОпции)

	Если ТипЗнч(Значение) = Тип("Булево") Тогда
		Возврат Значение;
	КонецЕсли;

	Написанное = СокрЛП(Строка(Значение));

	Если ПустаяСтрока(Написанное) Тогда
		Возврат Ложь;
	КонецЕсли;

	Если Написанное = "1" ИЛИ Написанное = "0" Тогда
		Возврат Написанное = "1";
	КонецЕсли;

	Попытка
		Возврат Булево(Написанное);
	Исключение
		ВызватьИсключение СтрШаблон(
			"Опция <--%1> — флаг: она либо указана, либо нет. Значение <%2> не понято,"
			+ " из значений принимаются только 1 и 0, да и нет, true и false",
			ИмяОпции, Написанное);
	КонецПопытки;

КонецФункции

Процедура ОписатьОпцииДоверия(Знач КомандаПриложения)

	КомандаПриложения.Опция("oidc-audience", "", "Аудитория id-token (клейм aud). По умолчанию — адрес хаба,
							|    в который публикуем: хаб сверяет её и отказывает при несовпадении.")
							.ВОкружении("OPM_OIDC_AUDIENCE");
	КомандаПриложения.Опция("oidc-token-var", "", "Имя переменной среды с готовым id-token.
							|    Без неё id-token ищется в переменной id_token, а в GitHub Actions
							|    заказывается у сервиса токенов конвейера.")
							.ВОкружении("OPM_OIDC_TOKEN_VAR");

КонецПроцедуры

// Значение устаревшей опции <-c|--channel>: имя канала либо auto.
// Проверяется сразу, чтобы негодное имя остановило команду до поиска файла и цели.
Функция ИмяКаналаИзКоманды(Знач КомандаПриложения)

	Значение = СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("channel")));

	Если ПустаяСтрока(Значение) ИЛИ Значение = "auto" Тогда
		Возврат "auto";
	КонецЕсли;

	Лог.Предупреждение("Опция <-c|--channel> устарела: канал версии хаб определяет тегами,"
		+ " а адрес пула публикации указывается опцией <--hub>.");

	Если НЕ ЭтоДопустимыйКаналПубликации(Значение) Тогда
		ВызватьИсключение "Указано недопустимое имя канала. Допустимые имена:" + Символы.ПС
			+ ДопустимыеИменаКаналовЗначения();
	КонецЕсли;

	Возврат Значение;

КонецФункции

// Маска файла пакета. Адрес хаба аргументом не принимается — только опцией <--hub>.
Функция МаскаФайлаПакетаИзКоманды(Знач КомандаПриложения)

	Маска = СокрЛП(Строка(КомандаПриложения.ЗначениеАргумента("FILE")));
	Устаревшая = СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("file")));

	Если НЕ ПустаяСтрока(Устаревшая) Тогда
		Лог.Предупреждение("Опция <-f|--file> устарела и будет удалена. Используйте аргумент <FILE>.");
		Если ЗначениеЗаполнено(Маска) Тогда
			ВызватьИсключение СтрШаблон("Файл пакета указан дважды: <%1> и <%2>", Маска, Устаревшая);
		КонецЕсли;
		Маска = Устаревшая;
	КонецЕсли;

	Если АдресаХаба.ЭтоАдресХаба(Маска) Тогда
		ВызватьИсключение СтрШаблон(
			"Аргумент <%1> похож на адрес хаба, а не на файл пакета."
			+ " Адрес публикации указывается опцией: opm push --hub %1", Маска);
	КонецЕсли;

	Возврат ?(ПустаяСтрока(Маска), "*.ospx", Маска);

КонецФункции

// Токен, названный в самой команде: стандартный ввод либо опция.
Функция ТокенИзКоманды(Знач КомандаПриложения)

	Если КомандаПриложения.ЗначениеОпции("token-stdin") Тогда
		Значение = "";
		ВвестиСтроку(Значение);
		Возврат СокрЛП(?(Значение = Неопределено, "", Значение));
	КонецЕсли;

	Возврат СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("token")));

КонецФункции

Функция ТокенПубликацииИзНастроек()

	НастройкиИзФайла = ПараметрыПриложенияOpm.СобратьНастройкиИзФайлов();

	Если НастройкиИзФайла.Свойство("ТокенПубликацииПакетов") Тогда
		Возврат СокрЛП(Строка(НастройкиИзФайла.ТокенПубликацииПакетов));
	КонецЕсли;

	Возврат "";

КонецФункции

Функция ПараметрыДоверия(Знач КомандаПриложения)

	Возврат Новый Структура("Аудитория, ИмяПеременной",
		СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("oidc-audience"))),
		СокрЛП(Строка(КомандаПриложения.ЗначениеОпции("oidc-token-var"))));

КонецФункции

// Объяснение конвейера доходит до человека в отказе «нужен токен», а не только в отладке.
Функция ПричинаОтсутствияДоверия(Знач ИмяПеременной, Знач Конвейер)

	Попытка
		Источник = ?(Конвейер = Неопределено, Новый ТокенКонвейераCI(), Конвейер);
		Возврат Источник.Обнаружить(ИмяПеременной).Причина;
	Исключение
		Возврат "конвейер с поддержкой id-token не опрошен";
	КонецПопытки;

КонецФункции

Функция НоваяАвторизация(Знач Заголовок, Знач Значение, Знач Доверенная)

	Возврат Новый Структура("Заголовок, Значение, Доверенная", Заголовок, Значение, Доверенная);

КонецФункции

// Совет по токену зависит от того, куда публикуем: OpenHub выдаёт токен по opm login,
// а легаси-хаб принимает токен GitHub, никакого входа для него нет.
Функция ГдеВзятьТокен(Знач Цель)

	Если Цель.Легаси Тогда
		Возврат "укажите <--token> либо переменную среды GITHUB_OAUTH_TOKEN";
	КонецЕсли;

	Возврат СтрШаблон("выполните opm login %1 либо укажите <--token>", Цель.БазовыйАдрес);

КонецФункции

// Легаси-хаб читает токен из заголовка OAUTH-TOKEN и понимает под ним токен GitHub,
// OpenHub — Bearer в Authorization. Различает их маршрут публикации, а не сам токен.
Функция АвторизацияТокеном(Знач Токен, Знач Цель)

	Значение = СокрЛП(Строка(Токен));

	Если Цель.Легаси Тогда
		Возврат НоваяАвторизация("OAUTH-TOKEN", Значение, Ложь);
	КонецЕсли;

	Если СтрНачинаетсяС(ВРег(Значение), "BEARER ") Тогда
		Возврат НоваяАвторизация("Authorization", Значение, Ложь);
	КонецЕсли;

	Возврат НоваяАвторизация("Authorization", "Bearer " + Значение, Ложь);

КонецФункции

Функция ЦельИзАдреса(Знач АдресХаба)

	РазобранныйАдрес = АдресаХаба.Разобрать(АдресХаба);

	Если ЗначениеЗаполнено(РазобранныйАдрес.ИмяПакета) Тогда
		ВызватьИсключение СтрШаблон(
			"Адрес публикации <%1>: ожидался адрес хаба или пула, а не пакета."
			+ " Уберите имя пакета из адреса", АдресХаба);
	КонецЕсли;

	АдресаХаба.ПредупредитьОбОткрытомКанале(РазобранныйАдрес);

	Цель = НоваяЦель("опция <--hub>", Ложь);
	Цель.Имя = АдресаХаба.ИмяСервераДляАдреса(РазобранныйАдрес);
	Цель.Сервер = СтрШаблон("%1://%2", РазобранныйАдрес.Схема, РазобранныйАдрес.Хост);
	Цель.Порт = АдресаХаба.ЭффективныйПорт(РазобранныйАдрес.Схема, РазобранныйАдрес.Порт);
	Цель.Ресурс = АдресаХаба.ПутьПубликации(РазобранныйАдрес.Пул);
	Цель.Пул = РазобранныйАдрес.Пул;
	Цель.БазовыйАдрес = РазобранныйАдрес.БазовыйАдрес;
	Цель.Авторизация = НастройкиOpm.АвторизацияДляАдреса(РазобранныйАдрес);

	Возврат Цель;

КонецФункции

// Сервер без маршрута публикации (например, файловое зеркало) — повод объясниться,
// а не упасть на обращении к свойству, которого у него не заведено.
Процедура ПроверитьПригодностьКПубликации(Знач НастройкаСервера, Знач ИмяСервераПакетов)

	Если НЕ ПустаяСтрока(РесурсПубликацииНастройки(НастройкаСервера)) Тогда
		Возврат;
	КонецЕсли;

	ВызватьИсключение СтрШаблон(
		"Сервер <%1> из opm.cfg публикацию не принимает: маршрута публикации у него нет."
		+ " Публикуйте в хаб — его адрес указывается опцией: opm push --hub [схема://]хост[:порт]/пул",
		ИмяСервераПакетов);

КонецПроцедуры

Функция ЦельИзНастройки(Знач НастройкаСервера, Знач Источник)

	Пул = АдресаХаба.ПулИзПутиПубликации(Строка(НастройкаСервера.РесурсПубликацииПакетов));

	Цель = НоваяЦель(Источник, ПустаяСтрока(Пул) И ЭтоЛегасиХаб(НастройкаСервера.Сервер));
	Цель.Имя = НастройкаСервера.Имя;
	Цель.Сервер = НастройкаСервера.Сервер;
	Цель.Порт = ПортИзНастройкиСервера(НастройкаСервера);
	Цель.Ресурс = Строка(НастройкаСервера.РесурсПубликацииПакетов);
	Цель.Пул = Пул;
	Цель.БазовыйАдрес = БазовыйАдресНастройки(НастройкаСервера);
	Цель.Таймаут = НастройкаСервера.Таймаут;
	Цель.Авторизация = ?(ЗначениеЗаполнено(НастройкаСервера.Авторизация),
		Строка(НастройкаСервера.Авторизация), "");

	Если ЗначениеЗаполнено(НастройкаСервера.ДополнительныеЗаголовки) Тогда
		Цель.Заголовки = НастройкаСервера.ДополнительныеЗаголовки;
	КонецЕсли;

	Возврат Цель;

КонецФункции

// Легаси-протокол — свойство хаба oscript.io: там токен читается из заголовка OAUTH-TOKEN,
// а канал версии обязателен. Прочие хабы говорят по OpenHub, где канал ведут теги пакета.
//
// Параметры:
//  АдресСервера - Строка - адрес сервера из настроек.
//
// Возвращаемое значение:
//  Булево.
//
Функция ЭтоЛегасиХаб(Знач АдресСервера) Экспорт

	Хост = ХостАдреса(АдресСервера);

	Если ПустаяСтрока(Хост) Тогда
		Возврат Ложь;
	КонецЕсли;

	Возврат Хост = ХостАдреса(КонстантыOpm.СерверУдаленногоХранилища)
		ИЛИ Хост = ХостАдреса(КонстантыOpm.СерверЗапасногоХранилища);

КонецФункции

Функция ХостАдреса(Знач Адрес)

	Попытка
		Возврат НРег(АдресаХаба.Разобрать(Строка(Адрес)).Хост);
	Исключение
		Возврат "";
	КонецПопытки;

КонецФункции

// Заготовка цели публикации, заполненная значениями хаба по умолчанию.
Функция НоваяЦель(Знач Источник, Знач Легаси)

	Возврат Новый Структура(
		"Имя, Сервер, Порт, Ресурс, Пул, БазовыйАдрес, Таймаут, Авторизация,
		|Заголовки, Источник, Легаси",
		КонстантыOpm.СерверУдаленногоХранилища, КонстантыOpm.СерверУдаленногоХранилища, 0,
		КонстантыOpm.РесурсПубликацииПакетов, "", КонстантыOpm.СерверУдаленногоХранилища,
		600, "", Новый Соответствие(), Источник, Легаси);

КонецФункции

// Аудитория id-token по умолчанию равна адресу хаба, поэтому у цели из настроек
// порт дописывается ровно тогда, когда он не подразумевается схемой.
Функция БазовыйАдресНастройки(Знач НастройкаСервера)

	Возврат АдресаХаба.АдресСПортом(СокрЛП(Строка(НастройкаСервера.Сервер)), НастройкаСервера.Порт);

КонецФункции

Функция НайтиНастройкуСервера(Знач ИмяСервераПакетов)

	Для Каждого НастройкаСервера Из НастройкиOpm.ПолучитьНастройки().СервераПакетов Цикл
		Если СтрСравнить(НастройкаСервера.Имя, ИмяСервераПакетов) = 0 Тогда
			Возврат НастройкаСервера;
		КонецЕсли;
	КонецЦикла;

	Возврат Неопределено;

КонецФункции

// Первый описанный сервер, который вообще принимает публикацию: у запасного зеркала
// и у файлового хранилища маршрута публикации нет.
Функция ПерваяНастройкаДляПубликации()

	Для Каждого НастройкаСервера Из НастройкиOpm.ПолучитьНастройки().СервераПакетов Цикл
		Если НЕ ПустаяСтрока(РесурсПубликацииНастройки(НастройкаСервера)) Тогда
			Возврат НастройкаСервера;
		КонецЕсли;
	КонецЦикла;

	Возврат Неопределено;

КонецФункции

Функция РесурсПубликацииНастройки(Знач НастройкаСервера)

	Попытка
		Значение = НастройкаСервера.РесурсПубликацииПакетов;
	Исключение
		Возврат "";
	КонецПопытки;

	Возврат ?(ЗначениеЗаполнено(Значение), СокрЛП(Строка(Значение)), "");

КонецФункции

// Поле Порт настройки сервера; порт из самого адреса главнее.
Функция ПортИзНастройкиСервера(Знач НастройкаСервера)

	Если НастройкаСервера.Порт = 0 Тогда
		Возврат 0;
	КонецЕсли;

	Попытка
		Разобранный = АдресаХаба.Разобрать(Строка(НастройкаСервера.Сервер));
	Исключение
		Возврат 0;
	КонецПопытки;

	Возврат ?(Разобранный.ПортЯвный, 0, НастройкаСервера.Порт);

КонецФункции

// Определяет значение заголовка CHANNEL.
// При публикации в пул канал не обязателен: OpenHub хранит его меткой версии,
// а состав каналов ведут теги пакета, поэтому неопределившийся auto даёт пустую метку.
// Легаси-маршрут публикации прежний: auto вне ветки master — ошибка.
Функция ПолучитьИмяКаналаПубликации(Знач ИмяКаналаПубликации, Знач КаналОбязателен)

	Если ИмяКаналаПубликации <> "auto" Тогда
		Возврат ИмяКаналаПубликации;
	КонецЕсли;

	КаналИзВетки = КаналИзГитРепозитория();

	Если ЗначениеЗаполнено(КаналИзВетки) Тогда
		Возврат КаналИзВетки;
	КонецЕсли;

	Если КаналОбязателен Тогда
		ВызватьИсключение "Невозможно автоматически определить канал. "
			+ "Требуется явное указание в параметре --channel одного из значений: " + ДопустимыеИменаКаналовЗначения();
	КонецЕсли;

	Возврат "";

КонецФункции

Функция КаналИзГитРепозитория()

	ГитРепозиторий = Новый ГитРепозиторий();
	ГитРепозиторий.УстановитьРабочийКаталог(ТекущийКаталог());

	Если НЕ ГитРепозиторий.ЭтоРепозиторий() Тогда
		Возврат "";
	КонецЕсли;

	Если ГитРепозиторий.ПолучитьТекущуюВетку() <> "master" Тогда
		Возврат "";
	КонецЕсли;

	Возврат ДопустимыеИменаКаналов.Стабильный;

КонецФункции

Функция ЭтоДопустимыйКаналПубликации(КаналПубликации)
	Результат = Ложь;
	Для Каждого КлючИЗначение Из ДопустимыеИменаКаналов Цикл
		Если КлючИЗначение.Значение = КаналПубликации Тогда
			Результат = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;

	Возврат Результат;
КонецФункции

// Принимает и маску в текущем каталоге, и путь к файлу — хоть относительный, хоть полный.
Функция ПолучитьФайлПакета(МаскаФайлаПакета)

	Указанный = Новый Файл(МаскаФайлаПакета);
	Если Указанный.Существует() И Указанный.ЭтоФайл() Тогда
		Возврат Указанный;
	КонецЕсли;

	Каталог = ТекущийКаталог();
	Маска = МаскаФайлаПакета;

	Если СтрНайти(Маска, ПолучитьРазделительПути()) > 0 ИЛИ СтрНайти(Маска, "/") > 0 Тогда
		Каталог = Указанный.Путь;
		Маска = Указанный.Имя;
	КонецЕсли;

	НайденныеФайлы = НайтиФайлы(Каталог, Маска);

	Если НайденныеФайлы.Количество() = 0 Тогда
		ВызватьИсключение СтрШаблон("По маске файла пакета <%1> не найдено файлов", МаскаФайлаПакета);
	КонецЕсли;
	Если НайденныеФайлы.Количество() > 1 Тогда
		ВызватьИсключение СтрШаблон("По маске файла пакета <%1> найдено больше одного файла", МаскаФайлаПакета);
	КонецЕсли;

	Возврат НайденныеФайлы[0];

КонецФункции

Функция ДопустимыеИменаКаналовЗначения()

	Значения = Новый Массив;
	Для Каждого ИмяКанала Из ДопустимыеИменаКаналов Цикл
		Значения.Добавить(ИмяКанала.Значение);
	КонецЦикла;

	Возврат СтрСоединить(Значения, ", ");

КонецФункции

Лог = Логирование.ПолучитьЛог("oscript.app.opm");

ДопустимыеИменаКаналов = Новый Структура;
ДопустимыеИменаКаналов.Вставить("Стабильный", "stable");
ДопустимыеИменаКаналов.Вставить("Разработческий", "dev");
