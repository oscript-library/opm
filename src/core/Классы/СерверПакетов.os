Перем Лог;

Перем Имя Экспорт;
Перем Сервер Экспорт;
Перем ПутьНаСервере Экспорт;
Перем Порт Экспорт;
Перем Приоритет Экспорт;
Перем Соединение;
Перем РесурсПубликацииПакетов Экспорт;
Перем Авторизация Экспорт;
Перем ДополнительныеЗаголовки Экспорт;
Перем Таймаут Экспорт;
// Истина у сервера, собранного по адресу из командной строки: до такого хаба
// «не достучались» — это отказ команды, а не повод молча идти к следующему зеркалу.
Перем НазванЯвно Экспорт;
Перем ПоследняяОшибкаСети Экспорт;

Перем ПакетыХаба;

Процедура ПриСозданииОбъекта(Знач ИмяСервера, Знач АдресСервер, Знач ВходящийПутьНаСервере = "",
		Знач ВходящийРесурсПубликацииПакетов = "", Знач ВходящийПорт = 0, Знач ВходящийПриоритет = 0,
		Знач ВходящаяАвторизация = Неопределено, Знач Заголовки = Неопределено, Знач ВходящийТаймаут = 60)
	
	Имя = ИмяСервера;
	Сервер = АдресСервер;
	ПутьНаСервере = ВходящийПутьНаСервере;
	Порт = ВходящийПорт;
	Приоритет = ВходящийПриоритет;
	РесурсПубликацииПакетов = ВходящийРесурсПубликацииПакетов;
	Авторизация = ВходящаяАвторизация;
	ДополнительныеЗаголовки = Заголовки;
	Таймаут = ВходящийТаймаут;
	НазванЯвно = Ложь;
	ПоследняяОшибкаСети = "";

КонецПроцедуры

// Адрес сервера в том виде, в каком его называют пользователю.
//
// Возвращаемое значение:
//  Строка - схема, хост и порт, если он не стандартный.
//
Функция АдресДляСообщений() Экспорт

	Возврат АдресаХаба.АдресСПортом(Сервер, Порт);

КонецФункции

Функция ПолучитьИмя() Экспорт
	Возврат Имя;
КонецФункции

Функция СерверДоступен() Экспорт
	Возврат Не Соединение = Неопределено;
КонецФункции

Функция ИнициализироватьСоединение()

	Если Не Соединение = Неопределено Тогда
		Возврат Соединение;
	КонецЕсли;
	
	Настройки = НастройкиOpm.ПолучитьНастройки();
	Если Настройки.ИспользоватьПрокси Тогда
		НастройкиПрокси = НастройкиOpm.ПолучитьИнтернетПрокси();
		Соединение = Новый HTTPСоединение(Сервер, Порт, , , НастройкиПрокси, Таймаут);
	Иначе
		Соединение = Новый HTTPСоединение(Сервер, Порт, , , , Таймаут);
	КонецЕсли;
	
	Возврат Соединение;
	
КонецФункции

// Получает ресурс с сервера.
//
// Параметры:
//  ИмяРесурса - Строка - имя файла относительно "Сервер/ПутьНаСервере".
//  Спецификация - Строка, Неопределено - то, что пользователь написал после @: точный номер
//                 версии, имя тега либо выражение версии. Уходит на хаб отдельным параметром
//                 запроса spec; Неопределено и пустая строка параметра не добавляют.
//
// Возвращаемое значение:
//  HTTPОтвет - ответ хаба; Неопределено, если запрос завершился исключением.
//
Функция ПолучитьРесурс(Знач ИмяРесурса, Знач Спецификация = Неопределено) Экспорт

	Соединение = ИнициализироватьСоединение();
	Ресурс = АдресРесурса(ИмяРесурса, Спецификация);
	Запрос = Новый HTTPЗапрос(Ресурс);
	ДобавитьЗаголовки(Запрос);

	Попытка

		ПоследняяОшибкаСети = "";
		Возврат Соединение.Получить(Запрос);

	Исключение

		// служебная обёртка платформы уходит в отладку, наружу — только суть
		ПодробноеОписание = ОписаниеОшибки();
		ПоследняяОшибкаСети = ОшибкиСети.Причина(ПодробноеОписание);
		Лог.Отладка("Обращение к %1%2 не удалось: %3", Сервер, Ресурс, ПодробноеОписание);
		Возврат Неопределено;

	КонецПопытки;

КонецФункции

// Адрес ресурса в запросе к хабу: путь выдачи с именем файла и спецификация
// отдельным параметром запроса.
//
// Параметры:
//  ИмяРесурса - Строка - имя файла относительно "Сервер/ПутьНаСервере".
//  Спецификация - Строка, Неопределено - строка после @; пустая даёт адрес без параметров.
//
// Возвращаемое значение:
//  Строка - путь и, если спецификация названа, строка запроса.
//
Функция АдресРесурса(Знач ИмяРесурса, Знач Спецификация = Неопределено) Экспорт

	Адрес = ПутьНаСервере + ПутьВАдресе(ИмяРесурса);

	Если НЕ ЗначениеЗаполнено(Спецификация) Тогда
		Возврат Адрес;
	КонецЕсли;

	Возврат СтрШаблон("%1?spec=%2", Адрес, БезопасноеДляАдреса(Спецификация));

КонецФункции

// Сегменты кодируются поодиночке: косая черта между ними — разделитель пути, а не символ имени.
Функция ПутьВАдресе(Знач ИмяРесурса)

	Сегменты = СтрРазделить(ИмяРесурса, "/", Истина);

	Для Индекс = 0 По Сегменты.ВГраница() Цикл
		Сегменты[Индекс] = БезопасноеДляАдреса(Сегменты[Индекс]);
	КонецЦикла;

	Возврат СтрСоединить(Сегменты, "/");

КонецФункции

// КодировкаURL оставляет нетронутыми только незарезервированные символы, поэтому годится
// и сегменту пути, и значению параметра. На кодирование платформой полагаться нельзя:
// плюс она оставляет как есть, а в строке запроса он читается пробелом.
Функция БезопасноеДляАдреса(Знач Часть)

	Возврат КодироватьСтроку(Часть, СпособКодированияСтроки.КодировкаURL);

КонецФункции

Функция ПрочитатьФайлСпискаПакетов(Текст)
	
	ТекстовыйДокумент  = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.УстановитьТекст(Текст);
	КоличествоПакетовВХабе = ТекстовыйДокумент.КоличествоСтрок();
	Для НомерСтроки = 1 По КоличествоПакетовВХабе Цикл
		ИмяПакета = СокрЛП(ТекстовыйДокумент.ПолучитьСтроку(НомерСтроки));
		
		Если ПустаяСтрока(ИмяПакета) Тогда
			Продолжить;
		КонецЕсли;

		Если ПакетыХаба[ИмяПакета] = Неопределено Тогда
			ПакетыХаба.Вставить(ИмяПакета, Новый Массив);
		КонецЕсли;
		ПакетыХаба[ИмяПакета] = ""; // Тут должна быть строка версий
	КонецЦикла;

КонецФункции

Функция ПолучитьСписокПакетов(Ресурс)

	Ответ = ПолучитьРесурс(Ресурс);
	Если Ответ = Неопределено Тогда
		ВызватьИсключение ОшибкиСети.НеУдалосьПодключиться(АдресДляСообщений(), ПоследняяОшибкаСети);
	КонецЕсли;

	Если Ответ.КодСостояния <> 200 Тогда
		КодСостояния = Ответ.КодСостояния;
		ТекстОтвета = Ответ.ПолучитьТелоКакСтроку();
		Ответ.Закрыть();
		ВызватьИсключение СтрШаблон("%1 ответил кодом %2 на запрос списка пакетов: %3",
			АдресДляСообщений(), Формат(КодСостояния, "ЧГ=0; ЧН=0"), СокрЛП(ТекстОтвета));
	КонецЕсли;
	ТекстОтвета = Ответ.ПолучитьТелоКакСтроку();
	Ответ.Закрыть();

	Возврат ТекстОтвета;

КонецФункции

Функция ПолучитьПакеты() Экспорт

	ПакетыХаба = Новый Соответствие;

	ТекстОтвета = "";

	Попытка
		ТекстОтвета = ПолучитьСписокПакетов("list.txt");
	Исключение
		// Адрес, названный в команде руками, — единственный источник пакета: до него не
		// достучались, значит команда провалилась. Перебор зеркал из конфигурации терпимее:
		// недоступное зеркало пропускается в пользу следующего.
		Если НазванЯвно Тогда
			ВызватьИсключение ОшибкиСети.Суть(ОписаниеОшибки());
		КонецЕсли;
		Лог.Предупреждение("Список пакетов зеркала %1 не получен: %2",
			Имя, ОшибкиСети.Суть(ОписаниеОшибки()));
	КонецПопытки;

	ПрочитатьФайлСпискаПакетов(ТекстОтвета);

	Возврат ПакетыХаба;

КонецФункции

Процедура ДобавитьЗаголовки(Знач Запрос)
	
	Если ЗначениеЗаполнено(ДополнительныеЗаголовки) Тогда
		Для Каждого мЗаголовок Из ДополнительныеЗаголовки Цикл
			ДобавитьЗаголовокКЗапросу(Запрос, мЗаголовок.Ключ, мЗаголовок.Значение);
		КонецЦикла;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Авторизация) Тогда
		ДобавитьЗаголовокКЗапросу(Запрос, "Authorization", Авторизация);
	КонецЕсли;

КонецПроцедуры

Процедура ДобавитьЗаголовокКЗапросу(Знач Запрос, Знач Заголовок, Знач Значение)
	Запрос.Заголовки.Вставить(Заголовок, ОбщегоНазначенияOpm.ПрименитьПеременныеСреды(Значение));
КонецПроцедуры

Функция НастройкаДляВыгрузки() Экспорт

	Результат = Новый Структура;
	Результат.Вставить("Имя", Имя);
	Результат.Вставить("Сервер", Сервер);
	Результат.Вставить("ПутьНаСервере", ПутьНаСервере);
	Результат.Вставить("РесурсПубликацииПакетов", РесурсПубликацииПакетов);
	Если ЗначениеЗаполнено(Порт) Тогда
		Результат.Вставить("Порт", Порт);
	КонецЕсли;
	Результат.Вставить("Таймаут", Таймаут);
	Результат.Вставить("Авторизация", Авторизация);
	Результат.Вставить("Заголовки", ДополнительныеЗаголовки);
	Результат.Вставить("Приоритет", Приоритет);
	
	Возврат Результат;

КонецФункции //

Лог = Логирование.ПолучитьЛог("oscript.app.opm");
ДополнительныеЗаголовки = Новый Соответствие;