// Переменные среды процесса. Отдельный объект, потому что от них зависят решения
// (какой PATH искать, есть ли вокруг конвейер) — в тестах он подменяется готовым набором.

Перем Значения;

Процедура ПриСозданииОбъекта(Знач ВходящиеЗначения = Неопределено)

	Значения = ?(ВходящиеЗначения = Неопределено, ПеременныеСреды(), ВходящиеЗначения);

КонецПроцедуры

// Значение переменной среды.
//
// Параметры:
//  Имя - Строка - имя переменной.
//
// Возвращаемое значение:
//  Строка - значение без краевых пробелов; пустая строка, если переменной нет.
//
Функция Значение(Знач Имя) Экспорт

	Прочитанное = Значения.Получить(Имя);

	Возврат ?(Прочитанное = Неопределено, "", СокрЛП(Строка(Прочитанное)));

КонецФункции

// Задана ли переменная среды непустым значением.
//
// Параметры:
//  Имя - Строка - имя переменной.
//
// Возвращаемое значение:
//  Булево.
//
Функция Задана(Знач Имя) Экспорт

	Возврат НЕ ПустаяСтрока(Значение(Имя));

КонецФункции
