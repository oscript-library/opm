Перем Лог;

Перем ИндексДоступныхПакетов;
Перем ИндексКешаПакетов;
Перем ИндексСерверовПакетов;

Процедура ПриСозданииОбъекта()

	Инициализировать();

КонецПроцедуры

// Скачивает файл пакета из хаба.
//
// Параметры:
//  ИмяПакета - Строка - имя пакета в хабе.
//  Спецификация - Строка, Неопределено - то, что пользователь написал после @: точный номер
//                 версии, имя тега либо выражение версии; Неопределено — версию выбирает хаб.
//  ПутьКФайлуПакета - Строка - куда положить скачанный файл; пустое даёт временный файл.
//  ИмяСервера - Строка - имя сервера пакетов; пустое означает перебор всех известных.
//
// Возвращаемое значение:
//  Строка - путь к скачанному файлу пакета.
//
Функция ПолучитьПакет(Знач ИмяПакета, Знач Спецификация, ПутьКФайлуПакета = "", ИмяСервера = "") Экспорт

	ОбновитьИндексыКешейПакетов();

	Если Не ПакетДоступен(ИмяПакета) Тогда
		ВызватьИсключение ОтказУстановки(ИмяПакета, "Пакет не найден");
	КонецЕсли;

	ИмяПакета = ОпределитьИмяАрхива(ИмяПакета);

	// пустая спецификация не схлопывается в Неопределено: «pkg@» — отказ, а не выбор хаба
	Если Спецификация <> Неопределено Тогда
		Спецификация = СокрЛП(Строка(Спецификация));
	КонецЕсли;

	ПроверитьВыразимостьСпецификации(ИмяПакета, Спецификация);
	ФайлПакета = ИмяФайлаПакета(ИмяПакета, Спецификация);

	ОбщегоНазначенияOpm.СообщениеПользователю(Лог, "Скачиваю файл: " + ФайлПакета);

	Если ПустаяСтрока(ПутьКФайлуПакета) Тогда
		ПутьКФайлуПакета = ВременныеФайлы.НовоеИмяФайла("ospx");
	КонецЕсли;

	ИмяРесурса = ИмяПакета + "/" + ФайлПакета;

	ПереченьСерверов = ИндексКешаПакетов[ИмяПакета];

	Полученное = ЗапроситьПакет(ПереченьСерверов, ИмяСервера, ИмяРесурса, Спецификация);
	Ответ = Полученное.Ответ;

	Если Ответ = Неопределено Тогда
		Если ЗначениеЗаполнено(Полученное.ОтказТранспорта) Тогда
			ВызватьИсключение Полученное.ОтказТранспорта;
		КонецЕсли;
		ВызватьИсключение ОтказУстановки(ИмяПакета,
			ПричинаНеудачи(ФайлПакета, Полученное.КодСостояния, Спецификация));
	КонецЕсли;

	Лог.Отладка("Файл получен");
	ЗаголовкиОтвета = ЗаголовкиОтвета(Ответ);
	Ответ.ПолучитьТелоКакДвоичныеДанные().Записать(ПутьКФайлуПакета);
	Ответ.Закрыть();
	Лог.Отладка("Соединение закрыто");

	ПроверитьЗагруженныйФайл(ПутьКФайлуПакета, ЗаголовкиОтвета, ФайлПакета);

	Возврат ПутьКФайлуПакета;

КонецФункции

// Файл, не прошедший сверку, на диске не оставляем: путь загрузки задаёт вызывающий,
// и непроверенный артефакт по этому пути — ровно то, от чего сверка защищает.
Процедура ПроверитьЗагруженныйФайл(Знач ПутьКФайлуПакета, Знач ЗаголовкиОтвета, Знач ФайлПакета)

	Попытка
		КонтрольныеСуммы.ПроверитьЗагрузку(ПутьКФайлуПакета, ЗаголовкиОтвета, ФайлПакета,
			НастройкиOpm.ПолучитьНастройки().ПроверкаКонтрольнойСуммы);
	Исключение
		УдалитьНепроверенныйФайл(ПутьКФайлуПакета);
		ВызватьИсключение;
	КонецПопытки;

КонецПроцедуры

// Не удалить файл — беда меньшая, чем потерять причину отказа сверки, поэтому
// исключение проверки наружу уходит в любом случае.
Процедура УдалитьНепроверенныйФайл(Знач ПутьКФайлу)

	Попытка
		УдалитьФайлы(ПутьКФайлу);
	Исключение
		Лог.Предупреждение("Не удалось удалить непроверенный файл %1: %2",
			ПутьКФайлу, ОписаниеОшибки());
	КонецПопытки;

КонецПроцедуры

// Имя артефакта остаётся легаси-формы и для тега: старый хаб ответит на него 404
// вместо молчаливой подмены последней версией.
Функция ИмяФайлаПакета(Знач ИмяПакета, Знач Спецификация)

	Если Спецификация = Неопределено Тогда
		Возврат ИмяПакета + ".ospx";
	КонецЕсли;

	Возврат СтрШаблон("%1-%2.ospx", ИмяПакета, Спецификация);

КонецФункции

// Косая черта в спецификации разрезала бы путь запроса — отказ до запроса.
Процедура ПроверитьВыразимостьСпецификации(Знач ИмяПакета, Знач Спецификация)

	Если Спецификация = Неопределено ИЛИ СтрНайти(Спецификация, "/") = 0 Тогда
		Возврат;
	КонецЕсли;

	ВызватьИсключение ОтказУстановки(ИмяПакета, СтрШаблон(
		"спецификация <%1> содержит косую черту."
		+ " Имя артефакта такую спецификацию не выражает — переименуйте тег либо укажите версию",
		Спецификация));

КонецПроцедуры

// Фасад отказа: все неудачи установки пакета выглядят для пользователя одинаково.
Функция ОтказУстановки(Знач ИмяПакета, Знач Причина)

	Возврат СтрШаблон("Ошибка установки пакета %1: %2", ИмяПакета, Причина);

КонецФункции

// Различает «хаб ответил, но файла нет» и «до хаба не достучались»: старое сообщение
// про отсутствие соединения на 404 сбивало с толку.
Функция ПричинаНеудачи(Знач ФайлПакета, Знач КодСостояния, Знач Спецификация)

	Если КодСостояния = 404 Тогда
		Возврат ПричинаОтсутствияАртефакта(ФайлПакета, Спецификация);
	ИначеЕсли КодСостояния = 401 ИЛИ КодСостояния = 403 Тогда
		Возврат СтрШаблон("хаб отказал в доступе к %1 (код %2). Нужен вход: opm login <адрес>",
			ФайлПакета, Формат(КодСостояния, "ЧГ=0; ЧН=0"));
	ИначеЕсли КодСостояния <> 0 Тогда
		Возврат СтрШаблон("хаб ответил кодом %1 на запрос %2",
			Формат(КодСостояния, "ЧГ=0; ЧН=0"), ФайлПакета);
	КонецЕсли;

	Возврат "Нет соединения";

КонецФункции

// Хаб сказал «нет такого артефакта». Теги и диапазоны разрешает только хаб,
// принимающий параметр spec, — на нём и стоит объяснение отказа.
Функция ПричинаОтсутствияАртефакта(Знач ФайлПакета, Знач Спецификация)

	Если ЗначениеЗаполнено(Спецификация) И НЕ РаботаСВерсиями.ЭтоТочныйНомерВерсии(Спецификация) Тогда
		Возврат СтрШаблон("хаб не умеет разрешать спецификацию <%1> и ответил 404."
			+ " По тегу и по выражению версии ставит только хаб, принимающий параметр spec;"
			+ " хабу без него нужен точный номер версии", Спецификация);
	КонецЕсли;

	Возврат СтрШаблон("файл %1 в хабе не найден", ФайлПакета);

КонецФункции

Функция ЗаголовкиОтвета(Знач Ответ)

	Попытка
		Возврат Ответ.Заголовки;
	Исключение
		Возврат Неопределено;
	КонецПопытки;

КонецФункции

// Текст отказа, когда пакет искали ровно на одном сервере и до него не дозвонились.
// Файловый сервер пакетов транспортной ошибки не имеет — у него пустая строка.
Функция ОтказЕдинственногоСервера(Знач Сервер)

	Попытка
		Причина = Сервер.ПоследняяОшибкаСети;
		Адрес = Сервер.АдресДляСообщений();
	Исключение
		Возврат "";
	КонецПопытки;

	Если НЕ ЗначениеЗаполнено(Причина) Тогда
		Возврат "";
	КонецЕсли;

	Возврат СтрШаблон("Не удалось подключиться к %1: %2", Адрес, Причина);

КонецФункции

// Возвращаемое значение:
//  Структура - Ответ (HTTPОтвет либо Неопределено), КодСостояния (Число, 0 — ответа не было),
//              ОтказТранспорта (Строка - готовый текст отказа явно названного сервера).
Функция ЗапроситьПакет(Знач ПереченьСерверов, Знач ИмяСервера, Знач ИмяРесурса, Знач Спецификация)

	ОтветСервера = Неопределено;
	КодСостояния = 0;
	ОтказТранспорта = "";

	// Если указан нужный сервер, то только его и будем использовать
	Если ЗначениеЗаполнено(ИмяСервера) Тогда

		Сервер = ИндексСерверовПакетов[ИмяСервера];
		Если Сервер = Неопределено Тогда
			ВызватьИсключение СтрШаблон("Сервер пакетов <%1> не описан в настройках", ИмяСервера);
		КонецЕсли;
		ОтветСервера = ЗапроситьПакетССервера(Сервер, ИмяРесурса, Спецификация, КодСостояния);

		// сервер один и назван руками: до него не достучались — это и есть исход команды
		Если ОтветСервера = Неопределено И КодСостояния = 0 Тогда
			ОтказТранспорта = ОтказЕдинственногоСервера(Сервер);
		КонецЕсли;

	Иначе

		// поиск пакета на серверах
		Для Каждого ДоступныйСервер Из ПереченьСерверов Цикл

			Сервер = ИндексСерверовПакетов[ДоступныйСервер.Ключ];
			ОтветСервера = ЗапроситьПакетССервера(Сервер, ИмяРесурса, Спецификация, КодСостояния);
			Если ОтветСервера <> Неопределено Тогда
				Прервать;
			КонецЕсли;

		КонецЦикла;

	КонецЕсли;

	Если ОтветСервера <> Неопределено Тогда
		Лог.Отладка("Ресурс %1 успешно получен с %2", ИмяРесурса, Сервер.ПолучитьИмя());
	КонецЕсли;

	Возврат Новый Структура("Ответ, КодСостояния, ОтказТранспорта",
		ОтветСервера, КодСостояния, ОтказТранспорта);

КонецФункции

Функция ЗапроситьПакетССервера(Сервер, ИмяРесурса, Знач Спецификация, КодСостояния = 0)

	ОтветСервера = Неопределено;
	Если Сервер.СерверДоступен() Тогда

		ОтветСервера = Сервер.ПолучитьРесурс(ИмяРесурса, Спецификация);
		Если ОтветСервера <> Неопределено Тогда

			КодСостояния = ОтветСервера.КодСостояния;

			Если ОтветСервера.КодСостояния = 200 Тогда

				Возврат ОтветСервера;

			КонецЕсли;

			ОтветСервера.Закрыть();

			// это ответ хаба, а не сбой связи: прежний текст «ошибка подключения» вводил в заблуждение
			Лог.Отладка("Хаб %1 ответил кодом %2 на запрос %3",
							Сервер.ПолучитьИмя(),
							КодСостояния,
							ИмяРесурса);

			ОтветСервера = Неопределено;

		КонецЕсли;

	КонецЕсли;

	Возврат ОтветСервера;

КонецФункции

// Функция по имени пакета определяет имя архива в хабе
// https://github.com/oscript-library/opm/issues/50
// Имена файлов в хабе регистрозависимы, однако имена пакетов по обыкновению регистронезависимы
Функция ОпределитьИмяАрхива(Знач ИмяПакета)

	Если ИндексДоступныхПакетов.Получить(ИмяПакета) = Неопределено Тогда

		Для Каждого мПакет Из ИндексДоступныхПакетов Цикл

			// Проводим регистронезависимое сравнение имён
			Если нрег(мПакет.Ключ) = нрег(ИмяПакета) Тогда

				// и возвращаем ровно то имя, которое хранится в хабе (с учётом регистра)
				Возврат мПакет.Ключ;

			КонецЕсли;

		КонецЦикла;

	КонецЕсли;

	Возврат ИмяПакета;

КонецФункции

Процедура Инициализировать()

	Лог.Отладка("Менеджер получения пакетов инициализация - НАЧАЛО");
	ОбновитьИндексСерверовПакетов();
	Лог.Отладка("Менеджер получения пакетов инициализация - ЗАВЕРШЕНО");

КонецПроцедуры

Функция ПолучитьДоступныеПакеты() Экспорт

	ОбновитьИндексыКешейПакетов();

	Возврат Новый ФиксированноеСоответствие(ИндексКешаПакетов);

КонецФункции

Функция ПакетДоступен(Знач ИмяПакета) Экспорт

	ОбновитьИндексыКешейПакетов();

	Если ИндексДоступныхПакетов.Получить(ИмяПакета) = Неопределено Тогда

		Для Каждого мПакет Из ИндексДоступныхПакетов Цикл

			// Проводим регистронезависимое сравнение имён
			Если нрег(мПакет.Ключ) = нрег(ИмяПакета) Тогда

				// и возвращаем ровно то имя, которое хранится в хабе (с учётом регистра)
				Возврат Истина;

			КонецЕсли;

		КонецЦикла;

	Иначе
		Возврат Истина;
	КонецЕсли;

	Возврат Ложь;

КонецФункции

Процедура ОбновитьИндексСерверовПакетов()

	ИндексСерверовПакетов = Новый Соответствие;

	Настройки = НастройкиOpm.ПолучитьНастройки();
	СервераПакетов = Настройки.СервераПакетов;

	Для каждого НастройкаСервера Из СервераПакетов Цикл

		ИндексСерверовПакетов.Вставить(НастройкаСервера.Имя, НастройкаСервера);

	КонецЦикла;

КонецПроцедуры

// ИменаДоступныхСерверов
//	Возвращает список доступных серверов-зеркал с пакетами
//  Возвращаемое значение:
//   Массив - Список имен
//
Функция ИменаДоступныхСерверов() Экспорт

	ИменаПакетов = Новый Массив();
	СервераПакетов = НастройкиOpm.ПолучитьНастройки().СервераПакетов;

	Для каждого НастройкаСервера Из СервераПакетов Цикл

		ИменаПакетов.Добавить(НастройкаСервера.Имя);

	КонецЦикла;

	Возврат ИменаПакетов;

КонецФункции

Процедура ОбновитьИндексДоступныхПакетов()

	// Учесть версии пакетов
	ИндексДоступныхПакетов = Новый Соответствие;

	Лог.Отладка("Обновляю кеш доступных пакетов");

	Для каждого ПакетКеша Из ИндексКешаПакетов Цикл

		ИндексДоступныхПакетов.Вставить(ПакетКеша.Ключ, Истина);

	КонецЦикла;

	Лог.Отладка("Кеш доступных пакетов - ОБНОВЛЕН");

КонецПроцедуры

Процедура ОбновитьИндексыКешейПакетов()
	Если Не ЗначениеЗаполнено(ИндексКешаПакетов)
		Или Не ЗначениеЗаполнено(ИндексДоступныхПакетов) Тогда
		ОбновитьИндексКешаПакетов();
		ОбновитьИндексДоступныхПакетов();
	КонецЕсли;
КонецПроцедуры

Процедура ОбновитьИндексКешаПакетов()

	ИндексКешаПакетов = Новый Соответствие;

	Для каждого ТекущийСерверПакетов Из ИндексСерверовПакетов Цикл

		ИмяСервера = ТекущийСерверПакетов.Ключ;
		КлассСервера = ТекущийСерверПакетов.Значение;

		Пакеты = КлассСервера.ПолучитьПакеты();

		Лог.Отладка("Добавляю в кеш пакеты <%2> сервера: %1", ИмяСервера, Пакеты.Количество());

		ДобавитьПакетыВИндексКеша(Пакеты, ИмяСервера);

	КонецЦикла;

КонецПроцедуры

Процедура ДобавитьПакетыВИндексКеша(Знач ПакетыСервера, Знач ИмяСервера)

	Для каждого Пакет Из ПакетыСервера Цикл

		КлючПакета = Пакет.Ключ;
		ВерсииПакета = Пакет.Значение;
		Лог.Отладка("Добавляю пакет: %1 в кеш для сервера %2", КлючПакета, ИмяСервера);
		Если ИндексКешаПакетов[КлючПакета] = Неопределено Тогда
			ИндексКешаПакетов.Вставить(КлючПакета, Новый Соответствие);
		КонецЕсли;

		ИндексКешаПакетов[КлючПакета].Вставить(ИмяСервера, ВерсииПакета)

	КонецЦикла;

КонецПроцедуры

Лог = Логирование.ПолучитьЛог("oscript.app.opm.receive");
